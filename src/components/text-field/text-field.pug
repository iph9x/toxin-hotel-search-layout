mixin text-field({ title, placeholderText, textValue, inputType='text' })
  .text-field
    if title
      .text-field__title= title
    input.text-field__input(type=inputType placeholder=placeholderText value=textValue)
