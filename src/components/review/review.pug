include ../like-button/like-button

mixin review({ src, name, date, text })
  .review
    img.review__icon(src=src)
    .review__user
      .review__name= name
      .review__date= date
    .review__like 
      +like-button({title: ''})
    .review__text= text
      
