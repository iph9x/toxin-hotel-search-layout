mixin subscription-text-field(props)
  .subscription-text-field
    if props.title
      .subscription-text-field__title= props.title
    .subscription-text-field__arrow arrow_forward
    input(type="text" placeholder=props.placeholderText class="subscription-text-field__input")