mixin toggle({ title, id, text })
  .toggle
    if (title)
      .toggle__title= title
    .toggle__item
      input.toggle__button(type="checkbox" id=id value=id name=id)
      label.toggle__label(for=id)= text