include ../components/header/header

block variables

doctype html
html(lang="ru")
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    meta(name="description" content="search and booking rooms")
    meta(name="keywords" content="toxin hotel search reservation rooms")
    title #{title}
  body
    +header({ isAuth: false })
    block content
    include ../components/footer/footer
    